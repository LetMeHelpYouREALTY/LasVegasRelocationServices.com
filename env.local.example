# =============================================================================
# Microsoft Clarity Configuration
# =============================================================================

# Microsoft Clarity - Free Heatmaps & Session Recording
# Get free account at: https://clarity.microsoft.com/
# Sign in with Microsoft account → Create project → Get Project ID
NEXT_PUBLIC_CLARITY_ID=your-clarity-project-id-here

# Google Search Console
# 1) Prefer Domain property + DNS TXT (covers apex + www)
# 2) Or HTML tag: GSC → Ownership verification → HTML tag → content= value only
# 3) Or upload google*.html into /public and redeploy
# After verify: GSC → Sitemaps → submit sitemap.xml
# Live checklist: https://www.lasvegasrelocationservices.com/api/seo-health
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Vercel Cron Jobs — random string ≥16 chars. Vercel sends it as:
#   Authorization: Bearer $CRON_SECRET
# Generate: openssl rand -hex 24
# Set in Vercel → Settings → Environment Variables → Production (do not commit).
CRON_SECRET=

# Follow Up Boss — NEVER commit a real key. A prior key was exposed in git
# history and MUST be rotated in FUB Admin → API, then set only in Vercel.
FOLLOW_UP_BOSS_API_KEY=your-follow-up-boss-api-key
FOLLOW_UP_BOSS_API_URL=https://api.followupboss.com/v1
FOLLOW_UP_BOSS_WEBHOOK_SECRET=your-fub-webhook-secret

# =============================================================================
# Example of what your .env.local should include:
# =============================================================================

# Copy this to your .env.local file:
# NEXT_PUBLIC_CLARITY_ID=abc123def456ghi789

# =============================================================================
# Quick Setup Steps:
# =============================================================================
# 1. Go to https://clarity.microsoft.com/
# 2. Sign in with your Microsoft account
# 3. Click "Create Project"
# 4. Enter project name: "Las Vegas Relocation Services"
# 5. Enter your website URL: https://lasvegasrelocationservices.com
# 6. Copy the Project ID (looks like: abc123def456ghi789)
# 7. Add to your .env.local: NEXT_PUBLIC_CLARITY_ID=abc123def456ghi789
# 8. Restart your development server
# 9. Check the browser console for "Microsoft Clarity initialized successfully"

# =============================================================================
# What You'll Get:
# =============================================================================
# ✅ Heatmaps showing where users click most
# ✅ Scroll depth analysis
# ✅ Session recordings (privacy-compliant)
# ✅ User behavior insights
# ✅ Performance metrics
# ✅ Form interaction tracking
# ✅ Custom event tracking for real estate
# ✅ All completely FREE with unlimited usage

# =============================================================================
# Cloudflare Configuration
# =============================================================================

# Cloudflare API Credentials
# Get these from your Cloudflare dashboard: https://dash.cloudflare.com/
NEXT_PUBLIC_CLOUDFLARE_ZONE_ID=48655987a584eb3068e3820930b6cd91
NEXT_PUBLIC_CLOUDFLARE_ACCOUNT_ID=2cc579c1ec9e426ed585e933ebf4753b
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-here

# Cloudflare Features (set to true to enable)
NEXT_PUBLIC_ENABLE_CLOUDFLARE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_CLOUDFLARE_SPEED_INSIGHTS=true
NEXT_PUBLIC_ENABLE_CLOUDFLARE_WEB_VITALS=true
NEXT_PUBLIC_ENABLE_HSTS=true
NEXT_PUBLIC_ENABLE_CSP=true
NEXT_PUBLIC_ENABLE_REFERRER_POLICY=true
NEXT_PUBLIC_ENABLE_CLOUDFLARE_IMAGES=true
NEXT_PUBLIC_WORKER_ENABLED=true
NEXT_PUBLIC_WORKER_ENVIRONMENT=production
NEXT_PUBLIC_ENABLE_EDGE_CACHING=true
NEXT_PUBLIC_ENABLE_EDGE_COMPUTING=true

# Cache TTL Settings (in seconds)
NEXT_PUBLIC_STATIC_CACHE_TTL=31536000  # 1 year
NEXT_PUBLIC_PAGE_CACHE_TTL=3600        # 1 hour
NEXT_PUBLIC_API_CACHE_TTL=300          # 5 minutes

# =============================================================================
# Quick Cloudflare Setup Steps:
# =============================================================================
# 1. Go to https://dash.cloudflare.com/
# 2. Add your domain: lasvegasrelocationservices.com
# 3. Update nameservers to point to Cloudflare
# 4. Go to API Tokens → Create Token
# 5. Use "Edit zone settings" template
# 6. Copy the API token to CLOUDFLARE_API_TOKEN above
# 7. Enable these features in Cloudflare dashboard:
#    - Auto Minify (HTML, CSS, JS)
#    - Brotli compression
#    - Early Hints
#    - Rocket Loader
#    - HSTS
#    - WAF (Web Application Firewall)
# 8. Deploy with: npm run deploy:cloudflare
